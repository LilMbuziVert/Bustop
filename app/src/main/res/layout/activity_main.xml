<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/appBackground"
    android:fitsSystemWindows="true"
    android:padding="16dp">

    <!-- Fixed Header: Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="?attr/appOnBackground"
        android:text="@string/nsw_bus_arrivals"
        style="@style/EInkText.Large"
        android:gravity="start"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="32dp"
        android:layout_marginStart="12dp"/>

    <!-- Fixed Header: Search Section -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="bottom"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:layout_marginBottom="48dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <EditText
                android:id="@+id/stopIdEditText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@null"
                style="@style/EInkText.SearchNumber"
                android:hint="2287141"
                android:textColorHint="?attr/appOnSurface"
                android:inputType="number"
                android:singleLine="true"
                android:padding="0dp"
                android:imeOptions="actionSearch"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:background="?attr/appOnBackground"
                android:layout_marginTop="4dp"/>
        </LinearLayout>

        <ImageView
            android:id="@+id/searchIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/search_24px"
            app:tint="?attr/appOnBackground"
            android:padding="8dp"
            android:layout_marginStart="8dp"
            android:layout_marginBottom="4dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"/>
    </LinearLayout>

    <!-- Fixed Header: Section Buttons -->
    <LinearLayout
        android:id="@+id/recentStopsSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="10dp"
        android:layout_marginStart="12dp"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/recentStopsButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/recent_stops"
            style="@style/EInkText.Medium"
            android:textColor="@android:color/black"
            app:drawableStartCompat="@drawable/directions_bus_24px"
            android:drawablePadding="8dp"
            app:drawableTint="@android:color/black"
            android:paddingVertical="10dp"
            android:paddingHorizontal="12dp"
            android:background="@drawable/recent_stops_background"
            android:clickable="true"
            android:focusable="true"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/nearestStopsButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text=""
            style="@style/EInkText.Medium"
            android:textColor="@android:color/black"
            app:drawableStartCompat="@drawable/my_location_24px"
            android:drawablePadding="0dp"
            app:drawableTint="@android:color/black"
            android:padding="10dp"
            android:background="@drawable/nearest_stops_background"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:textStyle="bold"/>
    </LinearLayout>

    <!-- CoordinatorLayout for Scrollable Carousel and Arrivals -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/appBackground"
            app:elevation="0dp">

            <!-- The Carousel scrolls away -->
            <LinearLayout
                android:id="@+id/carouselContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                app:layout_scrollFlags="scroll|enterAlways">

                <com.facebook.shimmer.ShimmerFrameLayout
                    android:id="@+id/recentStopsShimmer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:visibility="gone"
                    app:shimmer_auto_start="true"
                    app:shimmer_base_alpha="0.5"
                    app:shimmer_duration="1000">
                    <include layout="@layout/item_recent_stops_skeleton" />
                </com.facebook.shimmer.ShimmerFrameLayout>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recentStopsRecyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clipToPadding="false"
                    android:paddingHorizontal="0dp" />

                <LinearLayout
                    android:id="@+id/indicatorContainer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="start"
                    android:layout_marginTop="12dp"
                    android:layout_marginBottom="12dp"
                    android:orientation="horizontal"
                    android:paddingStart="12dp"/>
            </LinearLayout>

            <!-- The Filters stay sticky -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/busNumberRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="8dp"
                android:orientation="horizontal"
                android:paddingStart="12dp"
                android:paddingEnd="16dp"
                android:clipToPadding="false" />

        </com.google.android.material.appbar.AppBarLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
                android:id="@+id/swipeRefresh"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone"
                    android:scrollbars="vertical"
                    android:paddingStart="12dp"
                    android:paddingEnd="16dp"
                    android:scrollbarThumbVertical="@drawable/custom_scrollbar"
                    android:scrollbarStyle="outsideInset"
                    android:clipToPadding="false"/>
            </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

            <ProgressBar
                android:id="@+id/progressBar"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="center"
                android:indeterminateDrawable="@drawable/eink_progress_bar"
                android:visibility="gone" />

            <TextView
                android:id="@+id/errorTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="40dp"
                android:textColor="?attr/appErrorColor"
                android:textAlignment="center"
                android:visibility="gone" />

            <TextView
                android:id="@+id/noDataTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="40dp"
                android:text="@string/no_bus_arrivals_found_for_this_stop"
                android:textAlignment="center"
                android:visibility="gone" />

        </FrameLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</LinearLayout>