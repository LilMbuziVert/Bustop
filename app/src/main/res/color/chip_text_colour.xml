<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Text color when selected - uses theme attributes -->
    <item android:state_selected="true" android:color="?attr/appBackground" />
    <!-- Default text color - uses theme attributes -->
    <item android:color="?attr/appOnBackground" />
</selector>