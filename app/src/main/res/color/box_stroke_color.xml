<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused -->
    <item android:color="?attr/appOnSurface" android:state_focused="true" />
    <!-- Hovered -->
    <item android:color="?attr/appOnSurface" android:state_hovered="true" />
    <!-- Default (unfocused, not hovered) -->
    <item android:color="?attr/appOnSurface" />
</selector>
